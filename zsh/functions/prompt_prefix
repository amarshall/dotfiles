local h  # hostname
local u  # username
local c='%{%F{magenta}%}'  # color

[[ $USERNAME != 'amarshall' ]] && u='%n'
if $IS_REMOTE ; then
  [[ -n $u ]] && h='@'
  h="$h%m"
fi

[[ -n $h || -n $u ]] && printf "%s" "$c$u$h%{%b%f%}:"
