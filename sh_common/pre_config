# Adjust settings in this file for yourself. They are used for various
# configurations, such as if to display the username in the prompt.

# NOTE: Git has been instructed to always assume this file is unchanged to
# prevent conflicts when merging with upstream. You can still manually add the
# file (`git add sh_common/pre_config`). Or tell git to stop assuming it is
# unchanged (`git update-index --no-assume-unchanged sh_common/pre_config`).

export SH_USERNAME=amarshall
