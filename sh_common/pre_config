# Adjust settings in this file for yourself. They are used for various
# configurations, such as if to display the username in the prompt.

# NOTE: It's recommended to tell git to assume this file is unchanged to
# prevent conflicts when merging with upstream. Do this by running:
# `git update-index --assume-unchanged sh_common/pre_config`.

export SH_USERNAME=amarshall
